
#ifndef _FQUEUE_H_
#define _FQUEUE_H_  
struct fqentry{		/* One per process plus two per list	*/
	//int32	fqkey;		/* Key on which the queue is ordered	*/
	pid32 pid;
	struct fqentry* fqnext;		/* Index of next process or tail	*/
	//fqid16	fqprev;		/* Index of previous process or head	*/
};


#endif /* _FQUEUE_H_ */